'use strict';

describe('gameApp.loginView module', function() {

  beforeEach(module('gameApp.loginView'));

  describe('loginView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var loginViewCtrl = $controller('View1Ctrl');
      expect(loginViewCtrl).toBeDefined();
    }));

  });
});